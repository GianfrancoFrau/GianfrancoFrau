{
  "name": "gianfrancofrau",
  "private": true,
  "version": "3.0.0",
  "description": "Gianfranco Frau ~ my personal website",
  "scripts": {
    "build": "stencil build",
    "build:prod": "stencil build --prod",
    "setup": "./scripts/setup.sh",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate",
    "clean": "rm -rf www/*",
    "clean:modules": "rm -rf node_modules",
    "clean:all": "npm run clean && npm run clean:modules"
  },
  "devDependencies": {
    "@stencil-community/router": "^1.0.2",
    "@stencil/core": "2.13.0",
    "@stencil/sass": "^3.0.7",
    "@types/babel__traverse": "7.20.0",
    "@types/jest": "^27.0.3",
    "jest": "^27.4.5",
    "jest-cli": "^27.4.5",
    "puppeteer": "^10.0.0",
    "workbox-build": "^4.3.1"
  },
  "license": "MIT",
  "dependencies": {
    "@ionic/core": "^7.5.7"
  }
}
